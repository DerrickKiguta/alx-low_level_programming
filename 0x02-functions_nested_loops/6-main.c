#include <stdio.h>

int main(void)
{
    int a = -10;
    int b = 5;
    printf("The absolute value of %d is %d\n", a, _abs(a));
    printf("The absolute value of %d is %d\n", b, _abs(b));
    return (0);
}
